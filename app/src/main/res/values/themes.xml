<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_Lessons" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Android_Lessons" parent="Base.Theme.Android_Lessons" />
    
    <style name="Custom.MyCustomWidgetButton.Primary" parent="Widget.Material3.Button.IconButton.Filled">
        <item name="backgroundTint">#000</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Custom.MyCustomWidgetButton.Secondary" parent="Widget.Material3.Button.IconButton.Outlined">
        <item name="iconTint">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>